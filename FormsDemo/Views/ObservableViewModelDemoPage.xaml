<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="FormsDemo.Views.ObservableViewModelDemoPage">
    <StackLayout>
        <Slider Value="{Binding Red.Value}" />
        <Slider Value="{Binding Green.Value}" />
        <Slider Value="{Binding Blue.Value}" />

        <Label Text="{Binding ResultText}" />
        <BoxView Color="{Binding ResultColor.Value}" HeightRequest="100" />
    </StackLayout>
</ContentPage>